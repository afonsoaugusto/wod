# MongoDB Atlas connection string (do not commit the real one)
MONGODB_URI="mongodb+srv://USER:PASSWORD@cluster0.9qmia0k.mongodb.net/wod?retryWrites=true&w=majority&appName=Cluster0"

# Database name used by the app
MONGODB_DB="wod"

# Secret used by Auth.js to sign sessions (generate with: openssl rand -base64 32)
AUTH_SECRET="change-me"

# One-time token to create the first admin via POST /api/setup
SETUP_TOKEN="change-me-too"
